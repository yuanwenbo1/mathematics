---
layout: book
title: "高中数学 · 第 11 章：集合与常用逻辑用语"
description: "集合是确定对象的全体，对象叫元素。常用表示法有列举法、描述法和 Venn 图。"
permalink: /books/functions/chapter-11/
category: "高中数学"
book_id: functions
chapter_id: functions-chapter-11
prev_url: /books/functions/
prev_title: "高中数学全课程"
next_url: /books/functions/chapter-12/
next_title: "第 12 章：相等、不等与二次关系"
search: true
---

# 第 11 章：集合与常用逻辑用语

### 11.1 集合及其表示

集合是确定对象的全体，对象叫元素。常用表示法有列举法、描述法和 Venn 图。

```text
A = {1,2,3}
B = {x | x 是小于 5 的正整数}
```

`x ∈ A` 表示 `x` 属于 `A`，`C ⊆ A` 表示 `C` 是 `A` 的子集。空集记为 `∅`，它是任何集合的子集。

交集、并集、补集分别表示“同时属于”“至少属于一个”“在全集中但不属于该集合”：

```text
A ∩ B
A ∪ B
∁_U A
```

### 11.2 充分条件、必要条件与充要条件

若命题 `p -> q` 成立，则 `p` 是 `q` 的充分条件，`q` 是 `p` 的必要条件。若 `p -> q` 和 `q -> p` 都成立，则二者互为充要条件，记作 `p <-> q`。

例：“`x=2`”是“`x^2=4`”的充分条件，但不是必要条件，因为 `x=-2` 也满足结论。

### 11.3 全称量词与存在量词

```text
∀x∈A, p(x)：对 A 中所有 x，p(x) 成立
∃x∈A, p(x)：A 中至少存在一个 x，使 p(x) 成立
```

否定全称命题要改成存在命题并否定结论；否定存在命题要改成全称命题并否定结论。

```text
“所有实数的平方都大于 0”的否定：
“存在一个实数，它的平方不大于 0。”
```

反例 `x=0` 同时说明原命题为假。
