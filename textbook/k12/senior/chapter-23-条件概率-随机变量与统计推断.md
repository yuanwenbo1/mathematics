---
layout: book
title: "高中数学 · 第 23 章：条件概率、随机变量与统计推断"
description: "在 P(B)0 时："
permalink: /books/functions/chapter-23/
category: "高中数学"
book_id: functions
chapter_id: functions-chapter-23
prev_url: /books/functions/chapter-22/
prev_title: "第 22 章：计数原理、排列组合与二项式定理"
next_url: /books/functions/chapter-24/
next_title: "第 24 章：数学建模与数学探究"
search: true
---

# 第 23 章：条件概率、随机变量与统计推断

### 23.1 条件概率与全概率

在 `P(B)>0` 时：

```text
P(A|B)=P(A∩B)/P(B)
P(A∩B)=P(B)P(A|B)
```

若 `B1,...,Bn` 两两互斥并覆盖样本空间，则：

```text
P(A)=Σ P(Bi)P(A|Bi)
```

条件概率改变了样本空间。独立时 `P(A|B)=P(A)`，说明知道 `B` 不改变 `A` 的概率。

### 23.2 离散型随机变量

随机变量把随机结果映射成数。分布列列出所有可能值及概率，概率之和为 1。

```text
E(X)=Σ xi*pi
Var(X)=Σ (xi-E(X))^2*pi
```

期望表示长期平均水平，方差描述波动。`n` 次独立伯努利试验中成功次数 `X` 服从二项分布 `B(n,p)`，其期望为 `np`，方差为 `np(1-p)`。不放回抽样的成功次数常用超几何分布。

### 23.3 正态分布

正态分布由均值 `μ` 和标准差 `σ` 决定，曲线关于 `x=μ` 对称。`μ` 决定中心，`σ` 决定分散程度。标准化：

```text
Z=(X-μ)/σ
```

正态模型适合许多由大量小随机影响叠加产生的测量量，但使用前应检查数据形态和背景。

### 23.4 成对数据、回归与独立性检验

散点图用于观察两个变量关系，相关系数 `r` 描述线性相关方向和强度，`-1<=r<=1`。相关不等于因果。

一元线性回归用最小二乘法拟合 `ŷ=bx+a`，残差是实际值与预测值之差。模型评价要看散点、残差和适用范围，不能只看相关系数。

`2×2` 列联表可研究两个分类变量是否独立，依据卡方统计量进行独立性检验。统计结论是带有风险水平的判断，不是逻辑上的绝对证明。
