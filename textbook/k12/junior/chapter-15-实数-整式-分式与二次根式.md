---
layout: book
title: "初中数学 · 第 15 章：实数、整式、分式与二次根式"
description: "有理数可以写成两个整数之比 p/q（q != 0），包括整数、有限小数和无限循环小数。不能写成这种形式的数叫无理数，例如 √2、π。有理数和无理数合称实数。"
permalink: /books/restart/chapter-15/
category: "初中数学"
book_id: restart
chapter_id: restart-chapter-15
prev_url: /books/restart/chapter-07/
prev_title: "第 7 章：常见数学符号"
next_url: /books/restart/chapter-03/
next_title: "第 3 章：方程"
search: true
---

# 第 15 章：实数、整式、分式与二次根式

### 15.1 有理数与实数

有理数可以写成两个整数之比 `p/q`（`q != 0`），包括整数、有限小数和无限循环小数。不能写成这种形式的数叫无理数，例如 `√2`、`π`。有理数和无理数合称实数。

实数与数轴上的点一一对应。绝对值 `|a|` 表示数轴上点 `a` 到原点的距离，因此：

```text
|a| >= 0
|a| = |-a|
若 a >= 0，|a| = a
若 a < 0，|a| = -a
```

近似数必须说明精确程度。例如 `3.14` 精确到百分位；用近似值继续计算时，通常在最后一步再按要求取舍，避免误差过早累积。

### 15.2 幂、科学记数法与平方根

非零数的零次幂为 1，负整数指数表示倒数：

```text
a^m * a^n = a^(m+n)
(a^m)^n = a^(mn)
(ab)^n = a^n b^n
a^(-n) = 1/a^n  （a != 0）
```

科学记数法写成 `a * 10^n`，其中 `1 <= |a| < 10`。例如 `0.00042 = 4.2 * 10^(-4)`。

`√a` 只在 `a >= 0` 的实数范围内有意义，并表示非负平方根。`√(a^2) = |a|`，不能直接写成 `a`。

### 15.3 整式运算与因式分解

单项式和多项式统称整式。合并同类项只能合并字母部分完全相同的项。

```text
(a + b)^2 = a^2 + 2ab + b^2
(a - b)^2 = a^2 - 2ab + b^2
(a + b)(a - b) = a^2 - b^2
```

因式分解是把多项式写成若干整式乘积，常用方法包括提公因式、公式法和分组法。

例：

```text
2x^2 - 8 = 2(x^2 - 4) = 2(x - 2)(x + 2)
```

展开与因式分解互为逆过程，可以相互检验。

### 15.4 分式

形如 `A/B` 的式子叫分式，必须注明 `B != 0`。分式基本性质是分子、分母同乘或同除以同一个非零整式，分式值不变。

```text
1/x + 1/(x+1)
= (x+1)/(x(x+1)) + x/(x(x+1))
= (2x+1)/(x(x+1))
```

定义域限制为 `x != 0` 且 `x != -1`。约分不能消掉加法中的一项，例如 `(x+1)/x` 不能约成 1。

### 15.5 二次根式

当 `a >= 0` 时，`√a` 是二次根式。化简要把被开方数中的完全平方因数移到根号外：

```text
√12 = √(4*3) = 2√3
√8 + √18 = 2√2 + 3√2 = 5√2
```

只有化成最简二次根式后，被开方数相同的项才能合并。

### 15.6 本章验收

1. 将 `0.0000063` 写成科学记数法。
2. 化简 `(x+3)^2 - (x-3)(x+3)`。
3. 因式分解 `3x^2 - 12x`。
4. 化简 `√50 - √8`。
5. 写出分式 `(x+2)/(x^2-4)` 的取值限制并约分。

答案：`6.3*10^(-6)`；`6x+18`；`3x(x-4)`；`3√2`；`x != ±2`，在原限制下约为 `1/(x-2)`。
