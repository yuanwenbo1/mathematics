---
layout: book
title: "初中数学 · 第 16 章：方程、方程组与不等式"
description: "解方程的依据是等式性质。应用题的关键是设未知数、找等量关系、列方程、求解、检验并作答。"
permalink: /books/restart/chapter-16/
category: "初中数学"
book_id: restart
chapter_id: restart-chapter-16
prev_url: /books/restart/chapter-05/
prev_title: "第 5 章：坐标系"
next_url: /books/restart/chapter-17/
next_title: "第 17 章：初中函数"
search: true
---

# 第 16 章：方程、方程组与不等式

### 16.1 一元一次方程与应用

解方程的依据是等式性质。应用题的关键是设未知数、找等量关系、列方程、求解、检验并作答。

例：某商品先提价 20%，再打八折，现价 192 元。设原价为 `x`：

```text
x * 1.2 * 0.8 = 192
0.96x = 192
x = 200
```

“提价 20% 再降价 20%”不会回到原价，因为两次百分比所依据的基数不同。

### 16.2 二元一次方程组

方程组的解必须同时满足所有方程。消元法把二元问题转化为一元问题。

```text
x + y = 10
2x - y = 5
```

两式相加得 `3x = 15`，所以 `x = 5, y = 5`。也可以把每个方程看成一条直线，方程组的解就是两条直线的交点。

### 16.3 一元二次方程

一般形式：

```text
ax^2 + bx + c = 0  （a != 0）
```

解法包括因式分解、配方法和求根公式：

```text
x = (-b ± √(b^2 - 4ac)) / (2a)
```

判别式 `Δ = b^2 - 4ac`：`Δ > 0` 有两个不相等实根，`Δ = 0` 有两个相等实根，`Δ < 0` 在实数范围内无根。

根与系数关系：若根为 `x1, x2`，则 `x1+x2=-b/a`，`x1x2=c/a`。

### 16.4 分式方程

解分式方程时先确定所有分母不为 0 的限制，再乘最简公分母去分母。去分母可能产生增根，所以必须把结果代回原方程检验。

例：

```text
1/x = 2/(x+3)，x != 0,-3
x + 3 = 2x
x = 3
```

代回原式成立，因此 `x = 3` 是解。

### 16.5 一元一次不等式与不等式组

不等式两边同时乘或除以负数时，不等号方向必须改变：

```text
-2x > 6
x < -3
```

解不等式组就是求各个解集的公共部分。把解集画在数轴上可以减少边界错误；空心点表示不包含端点，实心点表示包含端点。

### 16.6 本章验收

1. 解方程组 `2x+y=7, x-y=2`。
2. 解 `x^2-5x+6=0`。
3. 解不等式组 `x>1, 2x<=8`。
4. 为什么分式方程必须验根？

答案：`x=3,y=1`；`x=2 或 3`；`1<x<=4`；去分母改变了方程形式，可能加入使原分母为 0 的值。
