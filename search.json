---
layout: null
permalink: /search.json
---
[
  {% assign searchable_pages = site.pages | where: "search", true %}
  {% for item in searchable_pages %}
  {
    "title": {{ item.title | jsonify }},
    "url": {{ item.url | relative_url | jsonify }},
    "category": {{ item.category | default: "教材" | jsonify }},
    "description": {{ item.description | default: "" | jsonify }},
    "content": {{ item.content | markdownify | strip_html | strip_newlines | jsonify }}
  }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]
